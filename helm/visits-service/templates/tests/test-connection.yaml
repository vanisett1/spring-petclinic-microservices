apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "visits-service.fullname" . }}-test-connection"
  labels:
    {{- include "visits-service.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "visits-service.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
